
    //abc formule
    int a = rand() % 20;
    int b = rand() % 20;
    int c = rand() % 20;

    if (a == 0) {
        a++;
    }
    else if (b == 0) {
        b++;
    }
    else if (c == 0) {
        c++;
    }

    // a x2 + b x + c = 0
    //cout << "equation: " << a << "x^2" << '+' << b << 'x' << '+' << c << " = 0";
